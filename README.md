# Old Robot Base Code
This repo is set up to house an example for the base code to analyze the data files from the older robot.

### Primary Files
1. DataRead
	2. This is the primary file that will be edited.
1. get_mvttimes
	2. This function determins the movement onset/offset and returns movement times.
1. Data
	2. Folder that should hold each subject folder, and then inside each subject folder there should be more for each condition.
Once code is cleaned up this will be instructions on what to edit.
